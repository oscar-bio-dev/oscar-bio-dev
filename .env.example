# Database (TimescaleDB via docker-compose)
DATABASE_URL=postgres://postgres:password@localhost:5432/oscar_bio_dev

# Artificial Intelligence (Cognitive Digital Twin)
GEMINI_API_KEY=your_gemini_key_here

# Server (defaults — override for production)
# PORT=3000
# APP_HOST=0.0.0.0

# Logging (tracing-subscriber EnvFilter)
# RUST_LOG=backend=info,tower_http=debug
